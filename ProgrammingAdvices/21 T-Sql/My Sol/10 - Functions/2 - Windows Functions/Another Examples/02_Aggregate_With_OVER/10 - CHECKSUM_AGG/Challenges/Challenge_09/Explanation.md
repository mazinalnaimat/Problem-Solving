# Explanation: Group and global utility-reading checksums together

## Approach

- One expression uses a local partition and the other uses the complete result set.
- Both values remain beside each detail row.

## Main ideas

- Partitioned and unpartitioned windows together
- Local versus global checksum

Review `Solution.sql` only after attempting the challenge yourself.
