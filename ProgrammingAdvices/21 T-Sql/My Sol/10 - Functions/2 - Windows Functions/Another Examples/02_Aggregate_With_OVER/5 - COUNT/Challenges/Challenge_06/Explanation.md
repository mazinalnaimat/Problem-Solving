# Explanation: Centered three-result race window

## Approach

- The frame is centered on the current result instead of always ending at it.
- Boundary rows naturally contain fewer rows because one neighbor does not exist.

## Main ideas

- A centered window frame
- Using both `PRECEDING` and `FOLLOWING`

Review `Solution.sql` only after attempting the challenge yourself.
