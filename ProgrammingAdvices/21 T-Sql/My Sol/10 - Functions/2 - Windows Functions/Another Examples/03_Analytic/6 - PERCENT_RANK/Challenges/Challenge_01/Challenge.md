# Challenge 1: PERCENT_RANK challenge 1: temperature inside each machine

## Task

Calculate the relative rank from 0 to 1 of temperature inside each machine while keeping every detail row.

## Requirements

- Use `PERCENT_RANK()`.
- Partition by `MachineCode`.
- Order by `TemperatureC`.

## Tables

- `dbo.MachineReadings`

## Setup

Run these files first:

1. `00_Database_Setup/01_AllInOne_Create_Database.sql`
2. `00_Database_Setup/03_Extended_Challenge_Data.sql`

## Try first

Write your query in `MySol.sql` before opening `Solution.sql`.
