# Challenge 3: CUME_DIST challenge 3: resolution time inside each support team

## Task

Calculate the cumulative distribution of resolution time inside each support team while keeping every detail row.

## Requirements

- Use `CUME_DIST()`.
- Partition by `TeamName`.
- Order by `ResolutionMinutes`.

## Tables

- `dbo.SupportTickets`

## Setup

Run these files first:

1. `00_Database_Setup/01_AllInOne_Create_Database.sql`
2. `00_Database_Setup/03_Extended_Challenge_Data.sql`

## Try first

Write your query in `MySol.sql` before opening `Solution.sql`.
