# Challenge 2: CUME_DIST challenge 2: delay inside each depot

## Task

Calculate the cumulative distribution of delay inside each depot while keeping every detail row.

## Requirements

- Use `CUME_DIST()`.
- Partition by `Depot`.
- Order by `DelayMinutes`.

## Tables

- `dbo.DeliveryTrips`

## Setup

Run these files first:

1. `00_Database_Setup/01_AllInOne_Create_Database.sql`
2. `00_Database_Setup/03_Extended_Challenge_Data.sql`

## Try first

Write your query in `MySol.sql` before opening `Solution.sql`.
