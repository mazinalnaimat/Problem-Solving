# Challenge 4: CUME_DIST challenge 4: quantity inside each inventory category

## Task

Calculate the cumulative distribution of quantity inside each inventory category while keeping every detail row.

## Requirements

- Use `CUME_DIST()`.
- Partition by `Category`.
- Order by `QuantityOnHand`.

## Tables

- `dbo.InventorySnapshots`

## Setup

Run these files first:

1. `00_Database_Setup/01_AllInOne_Create_Database.sql`
2. `00_Database_Setup/03_Extended_Challenge_Data.sql`

## Try first

Write your query in `MySol.sql` before opening `Solution.sql`.
