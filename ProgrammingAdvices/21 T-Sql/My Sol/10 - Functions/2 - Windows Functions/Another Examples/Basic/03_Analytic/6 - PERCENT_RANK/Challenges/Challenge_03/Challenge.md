# Challenge 3: PERCENT_RANK challenge 3: resolution time inside each support team

## Task

Calculate the relative rank from 0 to 1 of resolution time inside each support team while keeping every detail row.

## Requirements

- Use `PERCENT_RANK()`.
- Partition by `TeamName`.
- Order by `ResolutionMinutes`.

## Tables

- `dbo.SupportTickets`

## Setup

Run these files first:

1. `00_Database_Setup/01_AllInOne_Create_Database.sql`
2. `00_Database_Setup/03_Extended_Challenge_Data.sql`

## Try first

Write your query in `MySol.sql` before opening `Solution.sql`.
