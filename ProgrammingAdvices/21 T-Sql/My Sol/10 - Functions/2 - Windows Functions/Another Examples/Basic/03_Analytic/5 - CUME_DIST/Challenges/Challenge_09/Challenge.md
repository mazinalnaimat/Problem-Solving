# Challenge 9: CUME_DIST challenge 9: defect count inside each production line

## Task

Calculate the cumulative distribution of defect count inside each production line while keeping every detail row.

## Requirements

- Use `CUME_DIST()`.
- Partition by `ProductionLine`.
- Order by `DefectCount`.

## Tables

- `dbo.MachineReadings`

## Setup

Run these files first:

1. `00_Database_Setup/01_AllInOne_Create_Database.sql`
2. `00_Database_Setup/03_Extended_Challenge_Data.sql`

## Try first

Write your query in `MySol.sql` before opening `Solution.sql`.
