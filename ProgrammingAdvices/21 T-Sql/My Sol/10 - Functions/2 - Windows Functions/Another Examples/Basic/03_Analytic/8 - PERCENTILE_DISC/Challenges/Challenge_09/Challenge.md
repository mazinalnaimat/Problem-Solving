# Challenge 9: PERCENTILE_DISC challenge 9: 70th percentile machine output

## Task

Show each detail row and calculate the 70th percentile machine output for its partition.

## Requirements

- Use `PERCENTILE_DISC(0.70)`.
- Order the percentile by `OutputUnits`.
- Partition by `ProductionLine`.

## Tables

- `dbo.MachineReadings`

## Setup

Run these files first:

1. `00_Database_Setup/01_AllInOne_Create_Database.sql`
2. `00_Database_Setup/03_Extended_Challenge_Data.sql`

## Try first

Write your query in `MySol.sql` before opening `Solution.sql`.
