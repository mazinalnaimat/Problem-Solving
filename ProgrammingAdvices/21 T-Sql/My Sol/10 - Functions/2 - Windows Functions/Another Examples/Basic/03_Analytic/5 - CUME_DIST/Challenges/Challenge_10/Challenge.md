# Challenge 10: CUME_DIST challenge 10: satisfaction score inside each priority

## Task

Calculate the cumulative distribution of satisfaction score inside each priority while keeping every detail row.

## Requirements

- Use `CUME_DIST()`.
- Partition by `Priority`.
- Order by `SatisfactionScore`.

## Tables

- `dbo.SupportTickets`

## Setup

Run these files first:

1. `00_Database_Setup/01_AllInOne_Create_Database.sql`
2. `00_Database_Setup/03_Extended_Challenge_Data.sql`

## Try first

Write your query in `MySol.sql` before opening `Solution.sql`.
